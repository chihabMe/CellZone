{
  "name": "nextauth-hono",
  "version": "1.0.0",
  "scripts": {
    "dev:web": "pnpm --filter web dev",
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "pnpm --filter db  generate && pnpm --filter web build",
    "build:admin": "pnpm --filter db  generate && pnpm --filter admin build"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@adminjs/cli": "^1.0.5",
    "next": "14.2.4"
  },
  "devDependencies": {
    "nx": "^19.3.1"
  }
}
